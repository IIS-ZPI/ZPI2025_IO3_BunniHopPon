package com.bunihoppon.arithmetics.Div;

/// Any class implementing this interface must provide an implementation
/// for the Division method, which takes two double values and returns
/// the result of dividing the first value by the second.

interface IArithmeticsDiv {
    double Division(double A, double B);
}
